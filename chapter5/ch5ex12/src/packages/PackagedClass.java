//class is FRIENDLY so cannot be accessed from outside of the file/ same directory (as a default package)

package src.packages;

class PackagedClass{
	public PackagedClass(){
		System.out.println("Creating a packaged class");
	}
}